name: ping
description: Subscription Tracker — Know where your money goes.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  provider: ^6.1.0
  fl_chart: ^0.70.0
  google_fonts: ^6.2.0
  shared_preferences: ^2.3.0
  shimmer: ^3.0.0
  flutter_local_notifications: ^18.0.0
  url_launcher: ^6.3.0
  share_plus: ^10.0.0
  crypto: ^3.0.3
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
